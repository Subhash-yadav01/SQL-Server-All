/*
 
			 Displaying Data. 
			=>"SELECT" command is used to display data from table 
			=> We can display all rows or specific rows 
			=> We can display all cols or specific cols 
			Syntax:- 
			SELECT columns / * FROM tabname [Where cond] 
 
			SQL = ENGLISH 
			QUERIES = SENTENCE 
			CLAUSE = WORDS 
			*(star) = all columns 

			SEE ALL THE DETAILS ALL COLUMNS AND ROWS TO EMP TABLE
*/

			SELECT * FROM EMP